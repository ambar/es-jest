{
  "name": "es-jest",
  "version": "1.1.0",
  "description": "esbuild preprocessor for Jest",
  "repository": "ambar/es-jest",
  "license": "MIT",
  "engines": {
    "node": ">=10"
  },
  "main": "transform.js",
  "scripts": {
    "prepare": "npm test",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch --notify"
  },
  "keywords": [
    "jest",
    "esbuild",
    "testing"
  ],
  "files": [
    "jest-preset.js",
    "transform.js"
  ],
  "jest": {
    "preset": "./jest-preset"
  },
  "dependencies": {
    "esbuild": "^0.8.18"
  },
  "devDependencies": {
    "@types/jest": "^26.0.16",
    "jest": "^26.6.3",
    "noop3": "^1000.0.0",
    "prettier": "^2.2.1",
    "react": "^17.0.1"
  }
}
